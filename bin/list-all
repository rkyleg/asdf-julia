#!/usr/bin/env bash
versions=$(curl -s -0 https://api.github.com/repos/JuliaLang/julia/releases \
    | grep tag_name \
    | cut -d : -f 2 \
    | tr -d '"' \
    | tr -d , \
    | tr -d v \
    | tr '\n' ' ')
    # | cat -A
echo "$versions"
# echo ${#versions}